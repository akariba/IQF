Verification Round 3 — Adapter Lineage & Scaffold Provenance

Same standard as last time: every claim needs a direct quote + exact source, or an explicit "not confirmed." No inference stated as fact.

Question 1: What is the relationship between citi_adk_adapter and helix_adk_adapter?

Both are now independently confirmed real:

citi_adk_adapter — real GitHub repo (CitiInternalLS10/177199.digital-asst-app.citi-adk-adapter), real pip package (pip install citi-adk-adapter), with models.Vertex, models.Anthropic, models.Stellar. Maintainer: vd48527_citi.
helix_adk_adapter — referenced in the official techstrategy.citi.net/docs/genai/arc/adk_starter scaffold, which lists Anthropic[vertex] as a standard dependency and is what helix agent create python-adk-agent generates.

What's not yet known: are these the same underlying project under two names, is one a fork of the other, does one supersede the other, or are they genuinely independent/parallel efforts? Check:

Does citi_adk_adapter's README, CHANGELOG, or repo description mention helix_adk_adapter anywhere (as a predecessor, successor, or unrelated project)?
Does any Arc/Helix documentation page mention citi_adk_adapter by name?
Do the two packages' constants.py / default config values match exactly (project ID prj-gen-ai-9571, same Vertex endpoint, etc.), or differ in any way that would suggest they're diverging over time rather than being the same thing?
Is there a stated "current standard" anywhere — i.e., does Citi's GenAI platform team have a documented position on which adapter new projects should use?

Answer needed: Same lineage / fork / independent parallel efforts / unconfirmed — with the specific evidence for whichever it is. If unconfirmed, name who to ask (likely vd48527_citi for citi_adk_adapter, or whoever owns the Arc docs for helix_adk_adapter).

Question 2: Is stresstestingai a centrally-maintained internal scaffold, and has it been reviewed?

We've now found the same stresstestingai package structure (src/stresstestingai/fst/{ai,issuerShock,si,ui,utils,validation,langchain,llm,reporting_agent,scenarios}, same file names like marx.py, netCE_base.py, olymClient.py, xdrScenParser.py, same .env.example variable names) in two independent GitHub orgs:

CitiInternalLS10/167433.omc-gen-ai.polaris-poc (branch fst, contributor hz20691_citi)
CitiInternalLS4/172878.clientprofitability.stress-testing-ai (contributor unknown from what we've seen)

This strongly suggests a shared template rather than two people independently building the same thing from scratch. If true, this matters directly for the earlier open question about whether the LangChain+Tavily search pattern inside it is sanctioned — a template-level review would settle it for both repos at once, rather than needing to ask each repo owner separately.

Check:

Is there a canonical/origin repo for stresstestingai — a template repo, an internal cookiecutter/scaffold generator, or a platform team that distributes it?
Does either repo's README, commit history, or a linked wiki page identify it as based on a shared template, and if so, name the owning team?
Has that canonical template (if one exists) been through a security or architecture review — specifically regarding its TavilyClient/external search integration?
If no canonical source is found: are the two repos' .env.example and marx.py files identical, near-identical, or meaningfully different? (This affects whether they're truly the same lineage or just superficially similar.)

Answer needed: Confirmed shared template with named owner + review status / confirmed coincidental similarity / unconfirmed — with evidence either way. If unconfirmed, note whether it's worth directly comparing file hashes or diffing the two repos as a next step.
Output

European Call Price (1-step): $12.17
European Put Price (1-step): $7.28

I’d like to reach out because I’m working toward transitioning into model risk and validation, and your career path is very close to the direction I’m aiming for.

My background is in risk analytics across derivatives and capital markets. When working with models, I increasingly find myself wanting to understand not only the output but also the underlying design, mathematics, and implementation — and how to independently challenge them.


