You now have the COMPLETE Step-1 source set, including the real server/router files that were previously missing.

This is no longer an analysis task.

**Implement the Step-1 changes now and generate the complete downloadable files required for testing.**

# 1. SCOPE — STEP 1 ONLY

Implement only:

* Trigger 1 — Market Scanner
* Trigger 2 — User Narrative / R2D2
* existing 8-section Step-1 output
* guided enhancement/feedback capability
* Step-1 rescan/re-enrichment

Do NOT modify Steps 2–4 or unrelated RPR functionality.

---

# 2. IMPORTANT UI RULE

There are TWO HTML concepts:

### HTML v31

This is the **tech-lead-provided static baseline/reference design**.

It contains static/mock data and defines the approved visual design.

**DO NOT MODIFY HTML v31.**

### API-connected Step-1 HTML

This is the newer HTML derived from v31 but connected to the real backend APIs.

THIS is the HTML you may modify.

However:

* preserve the v31 appearance exactly
* no redesign
* no layout restructuring
* no new visual style
* preserve tabs, panels, eight-section accordions, feedback area, confirm/edit behaviour and general interaction model

Only make the minimum JavaScript/API/UI additions necessary for the enhancement feature.

---

# 3. FIRST RESOLVE THE REAL ACTIVE ROUTES

Now that the server/router files are uploaded, trace the actual imports and route registration.

Determine conclusively:

### Trigger 1

Which endpoint calls `MarketEventScout.fetch_events()`.

### Trigger 2

Which endpoint actually powers the API-connected Step-1 HTML.

Resolve whether the active implementation is:

`narrative_enricher.enrich_narrative()`

or

`RPRService.enrich_narrative()`

Do NOT maintain two enhancement implementations unnecessarily.

Modify the ACTIVE path.

Then register the enhancement router correctly in the real FastAPI application.

---

# 4. TRIGGER 1 — IMPROVE INITIAL RESULTS

Use the approved Trigger-1 prompt/specification where appropriate to improve the quality of the initial Market Scanner result.

Do not replace the existing ADK/web-search architecture.

Preserve:

`MarketEventScout.fetch_events()`

`_build_query()`

`MarketEventParser`

existing event IDs

existing retrieval mechanism.

Integrate the approved business requirements into the actual Trigger-1 execution in the smallest correct way.

The output should remain the same Step-1 8-section structure expected by the frontend.

Preserve integer sequential event IDs.

Do NOT introduce UUIDs.

---

# 5. TRIGGER 1 METADATA

Ensure the Trigger-1 result retains the metadata required for diagnosis:

* actual executed query
* retrieval status
* evidence tier per section
* technical retrieval failure where applicable
* enhancement metadata

Preserve the enhancement additions already correctly present in `market_event_scout.py`.

Fix only the issues identified in the architecture reconciliation.

Specifically correct `_placeholder()` defaults so placeholder events consistently contain:

* `enhancement_meta`
* `executed_query`

without relying on scattered post-assignment.

Do not redesign the class.

---

# 6. TRIGGER 2 — IMPROVE INITIAL RESULTS

Integrate the approved Step-1/R2D2 behaviour into the ACTUAL Trigger-2 implementation determined from the real route file.

Keep:

`R2D2_PROMPT`

as the production Trigger-2 prompt where it is genuinely used.

**Do not replace or permanently rewrite `R2D2_PROMPT`.**

Ensure Trigger 2 produces the same expected eight sections and enough enhancement metadata for diagnosis.

If `narrative_enricher.py` is the active implementation, preserve its existing enhancement metadata structure and make only the required corrections.

If `RPRService.enrich_narrative()` is actually active, integrate the enhancement contract there instead of creating a parallel unused implementation.

---

# 7. FIX THE CONFIRMED `None` BUG IF RELEVANT

The architecture analysis found that `RPRService.enrich_narrative()` can potentially continue after failed web retrieval with:

`web_content = None`

which can become literal `"None"` inside the Claude prompt.

If this function is part of the active Step-1 Trigger-2 path, fix it.

A technical retrieval failure must be represented explicitly.

Do NOT send `"None"` to Claude as though it were retrieved market evidence.

If `RPRService.enrich_narrative()` is not the active Step-1 path, do not modify it unnecessarily.

---

# 8. THE THREE APPROVED ENHANCEMENT PROMPTS

Create/use the three actual `.txt` prompt files:

* `trigger1_market_scanner.txt`
* `enhancement_diagnosis.txt`
* `feedback_consolidation.txt`

Use the exact approved prompt contents already uploaded.

Do NOT paraphrase them.

Place them in the exact prompt directory expected by the enhancement implementation.

From the previous analysis this was expected to be the `prompts/` directory associated with `rpr_enhancement_routes.py`, but now verify this against the actual project structure before generating files.

---

# 9. `trigger1_market_scanner.txt`

Previously this prompt existed as business/reference material but was not wired into the active Trigger-1 runtime.

Now determine the safest minimal way to use it to improve Trigger-1 output.

Do NOT create a second independent Trigger-1 engine.

The existing:

`web_search_agent.py`

and

`MarketEventScout`

architecture remains authoritative.

Integrate only where it genuinely improves the active Step-1 search/report behaviour.

---

# 10. ENHANCEMENT DIAGNOSIS

After an initial Trigger-1 or Trigger-2 Step-1 result has been produced, run:

`enhancement_diagnosis`

using the actual report plus actual search/retrieval metadata.

The diagnosis must distinguish:

### Evidence quality

Use the enum/value definitions already established in the real modified backend.

### Retrieval status

Distinguish success from technical failure.

Critical rule:

**TECHNICAL FAILURE ≠ NO EVIDENCE.**

Do not infer missing evidence merely because retrieval failed.

---

# 11. DIAGNOSIS BEHAVIOUR

If diagnosis returns:

`NOT_RECOMMENDED`

then:

* do not show an enhancement card
* do not rescan
* do not modify a prompt
* normal Step-1 workflow continues

If diagnosis returns:

`RECOMMENDED`

show the recommendation in the API-connected Step-1 HTML.

---

# 12. GUIDED FEEDBACK UI

Integrate the recommendation into the EXISTING feedback area of the API-connected HTML.

Preserve the v31 visual language.

Show a compact card similar to:

**Potential improvement detected**

Reason:
[diagnosis]

Suggested refinement:
[editable suggestion]

Actions:

* `Apply & Rescan`
* `Edit`
* `Dismiss`

Do not create another page.

Do not redesign the feedback area.

Do not change HTML v31 itself.

---

# 13. USER CONTROL

### Dismiss

* hide recommendation
* record it as dismissed for this Step-1 session
* no rescan
* no prompt modification

### Edit

* allow modification of the suggested refinement

### Apply & Rescan

Only after this explicit action:

1. send the approved/edited text to `feedback_consolidation`
2. obtain the final runtime refinement guidance
3. invoke the correct existing Trigger-1 or Trigger-2 refinement path
4. render the updated eight-section result

Nothing should run automatically after recommendation generation.

---

# 14. FEEDBACK CONSOLIDATION

Use:

`feedback_consolidation.txt`

exactly as approved.

Inputs must preserve:

* original user intent
* original theme/narrative/event context
* diagnosis
* suggested refinement
* final user-approved or edited feedback

The consolidated output is **runtime guidance only**.

Never permanently modify:

* `R2D2_PROMPT`
* ADK master prompt
* production source prompt files

---

# 15. TRIGGER 1 RESCAN

Use the existing:

`MarketEventScout.refine_event(...)`

path.

Do not create another Trigger-1 rescan engine.

Pass the consolidated user guidance through the existing feedback/refinement mechanism.

Preserve integer event IDs.

---

# 16. TRIGGER 2 RESCAN

Use the actual active Trigger-2 implementation identified from the server/router source.

Do not blindly append enhancement text to the narrative if that causes it to be treated incorrectly as original narrative.

Correct the previously identified `applyT2Rescan()` weakness.

Keep:

**original narrative**

separate from:

**runtime refinement guidance**

where the active backend architecture permits this.

The rescan should use the guidance to improve the search/enrichment, not corrupt or truncate the user's original narrative.

---

# 17. FIX THE T2 1000-CHARACTER TRUNCATION PROBLEM

The architecture analysis found the API-connected HTML's `applyT2Rescan()` could append enhancement context to the narrative textarea and then truncate to 1000 characters.

Fix this.

Do not truncate or overwrite the user's original narrative merely to insert enhancement guidance.

Preserve the complete original narrative and pass refinement guidance separately where possible.

If the real backend route needs a minimal request-model extension to support:

`narrative`

plus

`refinement`

implement that minimal change.

---

# 18. `rpr_enhancement_routes.py`

Regenerate this file based on the now-complete source set.

Correct:

* real PromptLoader/path behaviour
* exact approved prompt content/fallback behaviour
* enum comparisons
* actual Trigger-1 identifier types
* actual Trigger-2 active path
* actual response contracts
* rescan calls
* router registration assumptions

Do not use the previous generated file blindly.

Use the architecture reconciliation as guidance but the actual uploaded code as source of truth.

---

# 19. ROUTER REGISTRATION

Modify the actual uploaded FastAPI server/router registration file.

Add the enhancement router correctly.

For example conceptually:

`app.include_router(enhancement_router)`

but use the actual project import style and structure.

Do not invent a second application.

Verify that these enhancement endpoints are reachable.

---

# 20. EXISTING MANUAL FEEDBACK

Preserve the existing manual Step-1 feedback/editing capability.

The new guided recommendation is ADDITIONAL functionality.

Do not remove:

* manual section editing
* existing feedback textbox/icon
* Confirm Edit
* existing navigation behaviour

---

# 21. DUPLICATE RECOMMENDATIONS

Prevent the same recommendation from appearing repeatedly for the same event/narrative during the same Step-1 session.

Use lightweight state only.

No database.

Use the actual IDs available:

* Trigger-1 integer event ID + theme context
* appropriate Trigger-2 session/narrative scope

Do NOT invent `theme_id` UUIDs or `narrative_segment_id`.

---

# 22. DO NOT MODIFY THESE UNLESS NOW PROVEN NECESSARY

Prefer KEEP for:

* `llm_gateway.py`
* `prompt_loader.py`
* `r2d2_prompt.py`
* `web_search_agent.py`
* `evidence_service.py`

The previous analysis concluded these generally do not require Step-1 changes.

Only modify one if the now-complete source set proves it is technically necessary.

If unchanged, return it in the final status as:

`INSPECTED — NOT MODIFIED`

---

# 23. STATIC BASELINE PROTECTION

Again:

**HTML v31 must remain untouched.**

It is the tech-lead baseline.

The API-connected HTML may be modified only as necessary while preserving v31 design.

Do not overwrite the baseline file.

---

# 24. TEST THE REAL STEP-1 CONTRACT

Validate:

### Trigger 1

* scan endpoint reachable
* initial scan returns events
* eight sections render
* event IDs preserved
* diagnosis runs
* NOT_RECOMMENDED works
* RECOMMENDED card works
* Dismiss works
* Edit works
* Apply & Rescan works
* `refine_event()` receives consolidated guidance
* updated event renders

### Trigger 2

* actual active endpoint identified
* initial enrichment works
* eight sections render
* enhancement metadata available
* diagnosis runs
* NOT_RECOMMENDED works
* RECOMMENDED works
* Dismiss works
* Edit works
* Apply & Rescan works
* original narrative preserved
* updated result renders

### Existing UI

* manual editing works
* existing feedback still works
* Confirm works
* no visual redesign relative to v31

---

# 25. VALIDATION

Before delivering:

* run Python syntax checks
* verify imports
* verify FastAPI route registration
* verify route names against frontend calls
* verify prompt files can actually be loaded
* check JavaScript syntax
* check request/response contracts
* run available Step-1 tests
* add focused tests where reasonable

Do not claim runtime success for anything that could not actually be executed in this environment.

Clearly distinguish:

`PASSED`

`STATICALLY VERIFIED`

`NOT EXECUTABLE HERE`

---

# 26. GENERATE COMPLETE FILES

For every file changed, provide the COMPLETE final file.

Do not provide patches only.

For every new file, provide the COMPLETE file.

Preserve exact directory structure.

Do not regenerate unrelated files.

---

# 27. FINAL PACKAGE

Generate:

`rpr_step1_final_test_package.zip`

containing only the files required to deploy/test the updated Step-1 implementation.

Preserve folder structure.

Also generate:

`STEP1_FINAL_IMPLEMENTATION_SUMMARY.md`

containing:

* active Trigger-1 route
* active Trigger-2 route
* files modified
* files added
* files intentionally untouched
* prompt locations
* enhancement endpoints
* exact run/test instructions
* test results
* any remaining blocker

---

# 28. FINAL RESPONSE

Do not return another architecture analysis.

Return only:

1. **Implementation status**
2. **Resolved active Trigger-2 path**
3. **Modified files**
4. **New files**
5. **Files intentionally untouched**
6. **Prompt locations**
7. **Test results**
8. **Download links for individual files**
9. **Download link for `rpr_step1_final_test_package.zip`**
10. **Remaining blocker only if one genuinely exists**

The architecture-analysis phase is complete.

Implement now.


One additional delivery requirement:

If you modify **ANY existing file**, you must generate the **COMPLETE final version of that file as an individually downloadable artifact**.

Do not give me only:

* patches
* diffs
* snippets
* instructions describing what to change

For every file you modify, I want to download the complete replacement file and copy it directly into my project.

This applies to ALL file types, including:

* Python backend files
* server/router files
* API-connected Step-1 HTML
* JavaScript/CSS if separated
* prompt `.txt` files
* configuration files
* tests
* any other file changed during implementation

For every NEW file, also generate the complete file as an individually downloadable artifact.

At the end, provide a table:

| File     | Status                     | Destination        |
| -------- | -------------------------- | ------------------ |
| filename | MODIFIED / NEW / UNCHANGED | exact project path |

Rules:

* `MODIFIED` → MUST have an individual download
* `NEW` → MUST have an individual download
* `UNCHANGED` → do not regenerate unnecessarily
* Never tell me to manually edit a changed file
* Never omit a changed file from the downloads

Also package **all MODIFIED + NEW files** into:

`rpr_step1_final_test_package.zip`

with the correct project folder structure preserved.

Before finishing, cross-check the implementation against the file-change list and confirm that **every file marked MODIFIED or NEW has been generated and is downloadable**.
